# ACTIVE TASK

## Status

**CLOSED — UNITY AUTHORITATIVE SETTINGS + URP FOUNDATION**

No implementation task is currently active.

Repository:
`UglyGameFace/The-420-Lobby-Party-Night`

Merged PR:
#4 — `Build authoritative Unity settings and URP foundation`

Validated PR head:
`d43ef24e33f89dede7511f5fbd8caf4ffed20a1c`

Squash merge on `main`:
`c5fcb59546ce102c519e4b4703566f8de9e1991c`

## Completed scope

This task established the permanent Unity 6.3 LTS project/render foundation:

- Unity `6000.3.24f1 (4e7b9b5b6244)` remains pinned.
- `Packages/packages-lock.json` is source-controlled from the authoritative Unity capture.
- Party Night's URP Asset is source-controlled.
- Party Night's Universal Renderer is source-controlled.
- Party Night's URP Global Settings are source-controlled and registered.
- Party Night's default volume profile is source-controlled.
- Graphics, Quality, Editor and Project settings captured from Unity are source-controlled.
- every quality level inherits the project render pipeline.
- Force Text serialization is preserved.
- the editor assembly explicitly references Render Pipelines Core Runtime and Universal Runtime.
- the permanent validator checks editor/package/build-scene/render-pipeline/project-settings ownership.
- Edit Mode regression coverage calls the same permanent validator.
- static validation requires the package lock, Unity GUID relationships, assembly references and cleanup rules.

## Capture provenance

Unity Build Automation build #4 generated the authoritative settings capture on:

`9f6ade2d800547ebdea16e4cdaa3e4119efd8e1c`

The downloaded capture was verified before import:

- all 16 expected files present;
- all 16 SHA-256 checksums matched;
- imported Git blobs matched the captured Unity-generated bytes;
- the already committed `ProjectVersion.txt` and `EditorBuildSettings.asset` matched the captured copies.

Temporary capture/import plumbing was removed before final validation.

## Final validation

GitHub static workflow #38 passed on exact PR head:

`d43ef24e33f89dede7511f5fbd8caf4ffed20a1c`

Unity Build Automation build #6 then validated that same exact head:

- correct commit checked out;
- Unity `6000.3.24f1 (4e7b9b5b6244)` launched;
- pinned direct packages resolved;
- C# compilation passed;
- Party Night editor and Edit Mode test assemblies compiled;
- Edit Mode tests executed successfully;
- permanent committed-state validator passed during the test run;
- pre-export validator passed;
- Linux Player build completed with `Result: Success`;
- UBA ended with `Finished: SUCCESS`.

After merge, GitHub static workflow #39 passed on exact merged `main` commit:

`c5fcb59546ce102c519e4b4703566f8de9e1991c`

## Cleanup verification

The merged result contains no:

- `AuthoritativeSettingsBootstrap.cs`;
- artifact-capture callback;
- base64 transport chunks;
- one-shot import workflow;
- duplicate temporary URP assets;
- backup/temp copies;
- generated Unity working directories.

Git history remains the backup.

## Intentionally deferred

The following remain separate future tasks:

- Input Action asset and logical action map;
- native controller support across supported desktop/mobile/Web environments;
- touch controls for phones/tablets;
- player movement and interaction;
- authoritative multiplayer runtime;
- Hotbox Havoc gameplay;
- character/art asset evaluation;
- platform-specific quality/performance tuning;
- final company/store packaging identity.

## Next task candidate

**Cross-platform input foundation**

The next task should implement one shared logical input layer for:

`Move, Look, Jump, Interact, Grab, Dash, UseItem, Emote`

with keyboard/mouse, native gamepad/controller, and mobile touch bindings feeding the same actions without platform-specific gameplay forks.

Do not begin that implementation until it becomes the single active task.
